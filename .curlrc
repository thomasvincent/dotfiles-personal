# Prioritize TLS 1.2 or higher
tlsv1.2

# Set a reasonable connection timeout (adjust as needed)
connect-timeout = 60

# Additional Optional Settings (Uncomment and adjust as needed)

# # Set a cap on the file size to download:
# max-filesize = 50M

# # Specify proxy settings if you're behind a proxy:
# # proxy = http://myproxy.example.com:8080

# # Set your preferred DNS servers to improve name resolution:
# # dns-servers = 8.8.8.8,1.1.1.1
