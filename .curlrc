# Set User-Agent to disguise as IE 9 on Windows 7
user_agent="Mozilla/5.0 (compatible; MSIE 9.0; Windows NT 6.1; Trident/5.0)"

# Set referer to automatically set the previous URL when following a redirect
referer="auto"

# Set connection timeout to 60 seconds
connect_timeout=60
